# Dockerised learned baselines

- **poco** — runnable  (`baselines_ext/poco/`)
- **nksr** — runnable-with-risk  (`baselines_ext/nksr/`)
- **sap** — runnable-with-risk  (`baselines_ext/sap/`)
- **neuralpull** — runnable  (`baselines_ext/neuralpull/`)
- **convonet** — runnable-with-risk  (`baselines_ext/convonet/`)
- **capudf** — runnable-with-risk  (`baselines_ext/capudf/`)
